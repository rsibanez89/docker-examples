A docker-compose that installs worpress and mysql.

# To build
```
Run > docker-compose -f stack.yml up
```
# To remove
```
Run > docker volume rm 5-wordpress_db
Run > docker volume rm 5-wordpress
```